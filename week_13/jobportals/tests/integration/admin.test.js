// Integration tests for admin endpoints (placeholders)

describe('Admin API (integration) - placeholders', () => {
  test.skip('admin can create/update/delete vacancies', () => {});
  test.skip('admin can manage users', () => {});
});
